#include <iostream>
#include <string>
using namespace std;
int main() {
    string user_id;
    cin >> user_id;

    string surprise = user_id + "??!";

    cout << surprise << endl;

    return 0;
}